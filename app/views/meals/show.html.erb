<div class="card-trip flex " >
  <%= cl_image_tag @meal.photo.key, height: 300, width: 400, crop: :fill %>
  <%# <h1><%= @meal.name  </h1>%>
  <h3><%= @meal.description %></h3>
  <h1>£<%= @meal.price %></h1>
  <div class="positioning-text">
    <%= link_to 'Back', back_path, class: "btn btn-flat " %>
    <%= link_to 'Purchase', new_purchase_path, class: "btn btn-flat "%>
  </div>
</div>

